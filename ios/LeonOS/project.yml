name: LeonOS
options:
  bundleIdPrefix: app.leonos
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: 5.9
    DEVELOPMENT_TEAM: ""
targets:
  LeonOS:
    type: application
    platform: iOS
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.leonos.v6
        INFOPLIST_FILE: Info.plist
        GENERATE_INFOPLIST_FILE: NO
    scheme:
      testTargets: []
